services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: musico-bot
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped
    volumes:
      - ./data:/app/data